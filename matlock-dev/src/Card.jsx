import { useNavigate } from "react-router-dom";

function Card({ header, body, icons, thumbnail, id }) {
    const navigate = useNavigate();

    return (
        <div className="card">

        </div>
    );
}